// define a context object with initialvalue
// using 'createContext'

import {createContext} from 'react';
                            // iitial value is set to null
export const DataContext = createContext(null);